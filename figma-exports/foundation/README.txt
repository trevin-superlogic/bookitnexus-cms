Put the four "Foundation · Breakpoint" exports from Figma here:

  desktop.tokens.json
  wide desktop.tokens.json
  tablet.tokens.json
  mobile.tokens.json

Export them from Figma exactly as the Theme · Brand files were exported.
Filenames must match the above, including the space in "wide desktop".

Until these are present the import will skip the foundation layer and say so.
Everything else works without them.
