/**
 * Superseded — kept only so no stale import resolves to a half-built type.
 *
 * The fallback brand is not a bespoke document type: it is the universal
 * default of `brandTheme`, at `default.brandTheme`, exactly like
 * `default.experienceConfig` and `default.sharedContent`. See
 * `schemas/lib/scope.ts` for the pattern, and the `sourceTenant` field in
 * `schemas/documents/brandTheme.ts` that keeps the default in step with Bookit.
 *
 * This file exports nothing and is registered nowhere. Safe to delete.
 */
export {};
